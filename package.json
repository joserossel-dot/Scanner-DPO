{
  "name": "scanner-dpo-workspace",
  "version": "1.0.0",
  "description": "SaaS Platform for Website Privacy Scan & Consent CMP (Chile Law 21.719)",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "widget"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "dev:widget": "npm run dev -w widget",
    "build:widget": "npm run build -w widget",
    "build:frontend": "npm run build -w frontend && mkdir -p frontend/dist && cp widget/dist/widget.js frontend/dist/widget.js",
    "build:all": "npm run build:widget && npm run build:frontend",
    "start": "npm run start -w backend"
  },
  "dependencies": {
    "multer": "^2.2.0",
    "openai": "^7.4.0"
  }
}
